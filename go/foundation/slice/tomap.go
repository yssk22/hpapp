package slice

// ToMap convert a slice to a map with a key generated by the function
func ToMap[T any, S comparable](list []T, fun func(int, T) S) map[S]T {
	var m = make(map[S]T)
	for i := range list {
		key := fun(i, list[i])
		m[key] = list[i]
	}
	return m
}
