// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yssk22/hpapp/go/service/ent/metricdryrun"
	"github.com/yssk22/hpapp/go/service/ent/predicate"
)

// MetricDryRunQuery is the builder for querying MetricDryRun entities.
type MetricDryRunQuery struct {
	config
	ctx        *QueryContext
	order      []metricdryrun.Order
	inters     []Interceptor
	predicates []predicate.MetricDryRun
	modifiers  []func(*sql.Selector)
	loadTotal  []func(context.Context, []*MetricDryRun) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MetricDryRunQuery builder.
func (mdrq *MetricDryRunQuery) Where(ps ...predicate.MetricDryRun) *MetricDryRunQuery {
	mdrq.predicates = append(mdrq.predicates, ps...)
	return mdrq
}

// Limit the number of records to be returned by this query.
func (mdrq *MetricDryRunQuery) Limit(limit int) *MetricDryRunQuery {
	mdrq.ctx.Limit = &limit
	return mdrq
}

// Offset to start from.
func (mdrq *MetricDryRunQuery) Offset(offset int) *MetricDryRunQuery {
	mdrq.ctx.Offset = &offset
	return mdrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mdrq *MetricDryRunQuery) Unique(unique bool) *MetricDryRunQuery {
	mdrq.ctx.Unique = &unique
	return mdrq
}

// Order specifies how the records should be ordered.
func (mdrq *MetricDryRunQuery) Order(o ...metricdryrun.Order) *MetricDryRunQuery {
	mdrq.order = append(mdrq.order, o...)
	return mdrq
}

// First returns the first MetricDryRun entity from the query.
// Returns a *NotFoundError when no MetricDryRun was found.
func (mdrq *MetricDryRunQuery) First(ctx context.Context) (*MetricDryRun, error) {
	nodes, err := mdrq.Limit(1).All(setContextOp(ctx, mdrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{metricdryrun.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mdrq *MetricDryRunQuery) FirstX(ctx context.Context) *MetricDryRun {
	node, err := mdrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MetricDryRun ID from the query.
// Returns a *NotFoundError when no MetricDryRun ID was found.
func (mdrq *MetricDryRunQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mdrq.Limit(1).IDs(setContextOp(ctx, mdrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{metricdryrun.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mdrq *MetricDryRunQuery) FirstIDX(ctx context.Context) int {
	id, err := mdrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MetricDryRun entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MetricDryRun entity is found.
// Returns a *NotFoundError when no MetricDryRun entities are found.
func (mdrq *MetricDryRunQuery) Only(ctx context.Context) (*MetricDryRun, error) {
	nodes, err := mdrq.Limit(2).All(setContextOp(ctx, mdrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{metricdryrun.Label}
	default:
		return nil, &NotSingularError{metricdryrun.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mdrq *MetricDryRunQuery) OnlyX(ctx context.Context) *MetricDryRun {
	node, err := mdrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MetricDryRun ID in the query.
// Returns a *NotSingularError when more than one MetricDryRun ID is found.
// Returns a *NotFoundError when no entities are found.
func (mdrq *MetricDryRunQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mdrq.Limit(2).IDs(setContextOp(ctx, mdrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{metricdryrun.Label}
	default:
		err = &NotSingularError{metricdryrun.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mdrq *MetricDryRunQuery) OnlyIDX(ctx context.Context) int {
	id, err := mdrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MetricDryRuns.
func (mdrq *MetricDryRunQuery) All(ctx context.Context) ([]*MetricDryRun, error) {
	ctx = setContextOp(ctx, mdrq.ctx, "All")
	if err := mdrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MetricDryRun, *MetricDryRunQuery]()
	return withInterceptors[[]*MetricDryRun](ctx, mdrq, qr, mdrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mdrq *MetricDryRunQuery) AllX(ctx context.Context) []*MetricDryRun {
	nodes, err := mdrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MetricDryRun IDs.
func (mdrq *MetricDryRunQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mdrq.ctx.Unique == nil && mdrq.path != nil {
		mdrq.Unique(true)
	}
	ctx = setContextOp(ctx, mdrq.ctx, "IDs")
	if err = mdrq.Select(metricdryrun.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mdrq *MetricDryRunQuery) IDsX(ctx context.Context) []int {
	ids, err := mdrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mdrq *MetricDryRunQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mdrq.ctx, "Count")
	if err := mdrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mdrq, querierCount[*MetricDryRunQuery](), mdrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mdrq *MetricDryRunQuery) CountX(ctx context.Context) int {
	count, err := mdrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mdrq *MetricDryRunQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mdrq.ctx, "Exist")
	switch _, err := mdrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mdrq *MetricDryRunQuery) ExistX(ctx context.Context) bool {
	exist, err := mdrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MetricDryRunQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mdrq *MetricDryRunQuery) Clone() *MetricDryRunQuery {
	if mdrq == nil {
		return nil
	}
	return &MetricDryRunQuery{
		config:     mdrq.config,
		ctx:        mdrq.ctx.Clone(),
		order:      append([]metricdryrun.Order{}, mdrq.order...),
		inters:     append([]Interceptor{}, mdrq.inters...),
		predicates: append([]predicate.MetricDryRun{}, mdrq.predicates...),
		// clone intermediate query.
		sql:  mdrq.sql.Clone(),
		path: mdrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MetricDryRun.Query().
//		GroupBy(metricdryrun.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mdrq *MetricDryRunQuery) GroupBy(field string, fields ...string) *MetricDryRunGroupBy {
	mdrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MetricDryRunGroupBy{build: mdrq}
	grbuild.flds = &mdrq.ctx.Fields
	grbuild.label = metricdryrun.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.MetricDryRun.Query().
//		Select(metricdryrun.FieldCreatedAt).
//		Scan(ctx, &v)
func (mdrq *MetricDryRunQuery) Select(fields ...string) *MetricDryRunSelect {
	mdrq.ctx.Fields = append(mdrq.ctx.Fields, fields...)
	sbuild := &MetricDryRunSelect{MetricDryRunQuery: mdrq}
	sbuild.label = metricdryrun.Label
	sbuild.flds, sbuild.scan = &mdrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MetricDryRunSelect configured with the given aggregations.
func (mdrq *MetricDryRunQuery) Aggregate(fns ...AggregateFunc) *MetricDryRunSelect {
	return mdrq.Select().Aggregate(fns...)
}

func (mdrq *MetricDryRunQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mdrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mdrq); err != nil {
				return err
			}
		}
	}
	for _, f := range mdrq.ctx.Fields {
		if !metricdryrun.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mdrq.path != nil {
		prev, err := mdrq.path(ctx)
		if err != nil {
			return err
		}
		mdrq.sql = prev
	}
	if metricdryrun.Policy == nil {
		return errors.New("ent: uninitialized metricdryrun.Policy (forgotten import ent/runtime?)")
	}
	if err := metricdryrun.Policy.EvalQuery(ctx, mdrq); err != nil {
		return err
	}
	return nil
}

func (mdrq *MetricDryRunQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MetricDryRun, error) {
	var (
		nodes = []*MetricDryRun{}
		_spec = mdrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MetricDryRun).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MetricDryRun{config: mdrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(mdrq.modifiers) > 0 {
		_spec.Modifiers = mdrq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mdrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	for i := range mdrq.loadTotal {
		if err := mdrq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mdrq *MetricDryRunQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mdrq.querySpec()
	if len(mdrq.modifiers) > 0 {
		_spec.Modifiers = mdrq.modifiers
	}
	_spec.Node.Columns = mdrq.ctx.Fields
	if len(mdrq.ctx.Fields) > 0 {
		_spec.Unique = mdrq.ctx.Unique != nil && *mdrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mdrq.driver, _spec)
}

func (mdrq *MetricDryRunQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(metricdryrun.Table, metricdryrun.Columns, sqlgraph.NewFieldSpec(metricdryrun.FieldID, field.TypeInt))
	_spec.From = mdrq.sql
	if unique := mdrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mdrq.path != nil {
		_spec.Unique = true
	}
	if fields := mdrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, metricdryrun.FieldID)
		for i := range fields {
			if fields[i] != metricdryrun.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mdrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mdrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mdrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mdrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mdrq *MetricDryRunQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mdrq.driver.Dialect())
	t1 := builder.Table(metricdryrun.Table)
	columns := mdrq.ctx.Fields
	if len(columns) == 0 {
		columns = metricdryrun.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mdrq.sql != nil {
		selector = mdrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mdrq.ctx.Unique != nil && *mdrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mdrq.predicates {
		p(selector)
	}
	for _, p := range mdrq.order {
		p(selector)
	}
	if offset := mdrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mdrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MetricDryRunGroupBy is the group-by builder for MetricDryRun entities.
type MetricDryRunGroupBy struct {
	selector
	build *MetricDryRunQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mdrgb *MetricDryRunGroupBy) Aggregate(fns ...AggregateFunc) *MetricDryRunGroupBy {
	mdrgb.fns = append(mdrgb.fns, fns...)
	return mdrgb
}

// Scan applies the selector query and scans the result into the given value.
func (mdrgb *MetricDryRunGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mdrgb.build.ctx, "GroupBy")
	if err := mdrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MetricDryRunQuery, *MetricDryRunGroupBy](ctx, mdrgb.build, mdrgb, mdrgb.build.inters, v)
}

func (mdrgb *MetricDryRunGroupBy) sqlScan(ctx context.Context, root *MetricDryRunQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mdrgb.fns))
	for _, fn := range mdrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mdrgb.flds)+len(mdrgb.fns))
		for _, f := range *mdrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mdrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mdrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MetricDryRunSelect is the builder for selecting fields of MetricDryRun entities.
type MetricDryRunSelect struct {
	*MetricDryRunQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mdrs *MetricDryRunSelect) Aggregate(fns ...AggregateFunc) *MetricDryRunSelect {
	mdrs.fns = append(mdrs.fns, fns...)
	return mdrs
}

// Scan applies the selector query and scans the result into the given value.
func (mdrs *MetricDryRunSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mdrs.ctx, "Select")
	if err := mdrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MetricDryRunQuery, *MetricDryRunSelect](ctx, mdrs.MetricDryRunQuery, mdrs, mdrs.inters, v)
}

func (mdrs *MetricDryRunSelect) sqlScan(ctx context.Context, root *MetricDryRunQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mdrs.fns))
	for _, fn := range mdrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mdrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mdrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
