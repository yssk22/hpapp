// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yssk22/hpapp/go/service/ent/predicate"
	"github.com/yssk22/hpapp/go/service/ent/testent"
)

// TestEntQuery is the builder for querying TestEnt entities.
type TestEntQuery struct {
	config
	ctx        *QueryContext
	order      []testent.Order
	inters     []Interceptor
	predicates []predicate.TestEnt
	modifiers  []func(*sql.Selector)
	loadTotal  []func(context.Context, []*TestEnt) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TestEntQuery builder.
func (teq *TestEntQuery) Where(ps ...predicate.TestEnt) *TestEntQuery {
	teq.predicates = append(teq.predicates, ps...)
	return teq
}

// Limit the number of records to be returned by this query.
func (teq *TestEntQuery) Limit(limit int) *TestEntQuery {
	teq.ctx.Limit = &limit
	return teq
}

// Offset to start from.
func (teq *TestEntQuery) Offset(offset int) *TestEntQuery {
	teq.ctx.Offset = &offset
	return teq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (teq *TestEntQuery) Unique(unique bool) *TestEntQuery {
	teq.ctx.Unique = &unique
	return teq
}

// Order specifies how the records should be ordered.
func (teq *TestEntQuery) Order(o ...testent.Order) *TestEntQuery {
	teq.order = append(teq.order, o...)
	return teq
}

// First returns the first TestEnt entity from the query.
// Returns a *NotFoundError when no TestEnt was found.
func (teq *TestEntQuery) First(ctx context.Context) (*TestEnt, error) {
	nodes, err := teq.Limit(1).All(setContextOp(ctx, teq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{testent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (teq *TestEntQuery) FirstX(ctx context.Context) *TestEnt {
	node, err := teq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TestEnt ID from the query.
// Returns a *NotFoundError when no TestEnt ID was found.
func (teq *TestEntQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = teq.Limit(1).IDs(setContextOp(ctx, teq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{testent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (teq *TestEntQuery) FirstIDX(ctx context.Context) int {
	id, err := teq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TestEnt entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TestEnt entity is found.
// Returns a *NotFoundError when no TestEnt entities are found.
func (teq *TestEntQuery) Only(ctx context.Context) (*TestEnt, error) {
	nodes, err := teq.Limit(2).All(setContextOp(ctx, teq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{testent.Label}
	default:
		return nil, &NotSingularError{testent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (teq *TestEntQuery) OnlyX(ctx context.Context) *TestEnt {
	node, err := teq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TestEnt ID in the query.
// Returns a *NotSingularError when more than one TestEnt ID is found.
// Returns a *NotFoundError when no entities are found.
func (teq *TestEntQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = teq.Limit(2).IDs(setContextOp(ctx, teq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{testent.Label}
	default:
		err = &NotSingularError{testent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (teq *TestEntQuery) OnlyIDX(ctx context.Context) int {
	id, err := teq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TestEnts.
func (teq *TestEntQuery) All(ctx context.Context) ([]*TestEnt, error) {
	ctx = setContextOp(ctx, teq.ctx, "All")
	if err := teq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TestEnt, *TestEntQuery]()
	return withInterceptors[[]*TestEnt](ctx, teq, qr, teq.inters)
}

// AllX is like All, but panics if an error occurs.
func (teq *TestEntQuery) AllX(ctx context.Context) []*TestEnt {
	nodes, err := teq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TestEnt IDs.
func (teq *TestEntQuery) IDs(ctx context.Context) (ids []int, err error) {
	if teq.ctx.Unique == nil && teq.path != nil {
		teq.Unique(true)
	}
	ctx = setContextOp(ctx, teq.ctx, "IDs")
	if err = teq.Select(testent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (teq *TestEntQuery) IDsX(ctx context.Context) []int {
	ids, err := teq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (teq *TestEntQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, teq.ctx, "Count")
	if err := teq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, teq, querierCount[*TestEntQuery](), teq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (teq *TestEntQuery) CountX(ctx context.Context) int {
	count, err := teq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (teq *TestEntQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, teq.ctx, "Exist")
	switch _, err := teq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (teq *TestEntQuery) ExistX(ctx context.Context) bool {
	exist, err := teq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TestEntQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (teq *TestEntQuery) Clone() *TestEntQuery {
	if teq == nil {
		return nil
	}
	return &TestEntQuery{
		config:     teq.config,
		ctx:        teq.ctx.Clone(),
		order:      append([]testent.Order{}, teq.order...),
		inters:     append([]Interceptor{}, teq.inters...),
		predicates: append([]predicate.TestEnt{}, teq.predicates...),
		// clone intermediate query.
		sql:  teq.sql.Clone(),
		path: teq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		StringField string `json:"string_field,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TestEnt.Query().
//		GroupBy(testent.FieldStringField).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (teq *TestEntQuery) GroupBy(field string, fields ...string) *TestEntGroupBy {
	teq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TestEntGroupBy{build: teq}
	grbuild.flds = &teq.ctx.Fields
	grbuild.label = testent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		StringField string `json:"string_field,omitempty"`
//	}
//
//	client.TestEnt.Query().
//		Select(testent.FieldStringField).
//		Scan(ctx, &v)
func (teq *TestEntQuery) Select(fields ...string) *TestEntSelect {
	teq.ctx.Fields = append(teq.ctx.Fields, fields...)
	sbuild := &TestEntSelect{TestEntQuery: teq}
	sbuild.label = testent.Label
	sbuild.flds, sbuild.scan = &teq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TestEntSelect configured with the given aggregations.
func (teq *TestEntQuery) Aggregate(fns ...AggregateFunc) *TestEntSelect {
	return teq.Select().Aggregate(fns...)
}

func (teq *TestEntQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range teq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, teq); err != nil {
				return err
			}
		}
	}
	for _, f := range teq.ctx.Fields {
		if !testent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if teq.path != nil {
		prev, err := teq.path(ctx)
		if err != nil {
			return err
		}
		teq.sql = prev
	}
	return nil
}

func (teq *TestEntQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TestEnt, error) {
	var (
		nodes = []*TestEnt{}
		_spec = teq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TestEnt).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TestEnt{config: teq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(teq.modifiers) > 0 {
		_spec.Modifiers = teq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, teq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	for i := range teq.loadTotal {
		if err := teq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (teq *TestEntQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := teq.querySpec()
	if len(teq.modifiers) > 0 {
		_spec.Modifiers = teq.modifiers
	}
	_spec.Node.Columns = teq.ctx.Fields
	if len(teq.ctx.Fields) > 0 {
		_spec.Unique = teq.ctx.Unique != nil && *teq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, teq.driver, _spec)
}

func (teq *TestEntQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(testent.Table, testent.Columns, sqlgraph.NewFieldSpec(testent.FieldID, field.TypeInt))
	_spec.From = teq.sql
	if unique := teq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if teq.path != nil {
		_spec.Unique = true
	}
	if fields := teq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, testent.FieldID)
		for i := range fields {
			if fields[i] != testent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := teq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := teq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := teq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := teq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (teq *TestEntQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(teq.driver.Dialect())
	t1 := builder.Table(testent.Table)
	columns := teq.ctx.Fields
	if len(columns) == 0 {
		columns = testent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if teq.sql != nil {
		selector = teq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if teq.ctx.Unique != nil && *teq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range teq.predicates {
		p(selector)
	}
	for _, p := range teq.order {
		p(selector)
	}
	if offset := teq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := teq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TestEntGroupBy is the group-by builder for TestEnt entities.
type TestEntGroupBy struct {
	selector
	build *TestEntQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tegb *TestEntGroupBy) Aggregate(fns ...AggregateFunc) *TestEntGroupBy {
	tegb.fns = append(tegb.fns, fns...)
	return tegb
}

// Scan applies the selector query and scans the result into the given value.
func (tegb *TestEntGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tegb.build.ctx, "GroupBy")
	if err := tegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestEntQuery, *TestEntGroupBy](ctx, tegb.build, tegb, tegb.build.inters, v)
}

func (tegb *TestEntGroupBy) sqlScan(ctx context.Context, root *TestEntQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tegb.fns))
	for _, fn := range tegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tegb.flds)+len(tegb.fns))
		for _, f := range *tegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TestEntSelect is the builder for selecting fields of TestEnt entities.
type TestEntSelect struct {
	*TestEntQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tes *TestEntSelect) Aggregate(fns ...AggregateFunc) *TestEntSelect {
	tes.fns = append(tes.fns, fns...)
	return tes
}

// Scan applies the selector query and scans the result into the given value.
func (tes *TestEntSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tes.ctx, "Select")
	if err := tes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestEntQuery, *TestEntSelect](ctx, tes.TestEntQuery, tes, tes.inters, v)
}

func (tes *TestEntSelect) sqlScan(ctx context.Context, root *TestEntQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tes.fns))
	for _, fn := range tes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
