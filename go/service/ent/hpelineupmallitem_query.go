// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yssk22/hpapp/go/service/ent/hpartist"
	"github.com/yssk22/hpapp/go/service/ent/hpelineupmallitem"
	"github.com/yssk22/hpapp/go/service/ent/hpmember"
	"github.com/yssk22/hpapp/go/service/ent/predicate"
)

// HPElineupMallItemQuery is the builder for querying HPElineupMallItem entities.
type HPElineupMallItemQuery struct {
	config
	ctx                    *QueryContext
	order                  []hpelineupmallitem.Order
	inters                 []Interceptor
	predicates             []predicate.HPElineupMallItem
	withTaggedArtists      *HPArtistQuery
	withTaggedMembers      *HPMemberQuery
	modifiers              []func(*sql.Selector)
	loadTotal              []func(context.Context, []*HPElineupMallItem) error
	withNamedTaggedArtists map[string]*HPArtistQuery
	withNamedTaggedMembers map[string]*HPMemberQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HPElineupMallItemQuery builder.
func (hemiq *HPElineupMallItemQuery) Where(ps ...predicate.HPElineupMallItem) *HPElineupMallItemQuery {
	hemiq.predicates = append(hemiq.predicates, ps...)
	return hemiq
}

// Limit the number of records to be returned by this query.
func (hemiq *HPElineupMallItemQuery) Limit(limit int) *HPElineupMallItemQuery {
	hemiq.ctx.Limit = &limit
	return hemiq
}

// Offset to start from.
func (hemiq *HPElineupMallItemQuery) Offset(offset int) *HPElineupMallItemQuery {
	hemiq.ctx.Offset = &offset
	return hemiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hemiq *HPElineupMallItemQuery) Unique(unique bool) *HPElineupMallItemQuery {
	hemiq.ctx.Unique = &unique
	return hemiq
}

// Order specifies how the records should be ordered.
func (hemiq *HPElineupMallItemQuery) Order(o ...hpelineupmallitem.Order) *HPElineupMallItemQuery {
	hemiq.order = append(hemiq.order, o...)
	return hemiq
}

// QueryTaggedArtists chains the current query on the "tagged_artists" edge.
func (hemiq *HPElineupMallItemQuery) QueryTaggedArtists() *HPArtistQuery {
	query := (&HPArtistClient{config: hemiq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hemiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hemiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hpelineupmallitem.Table, hpelineupmallitem.FieldID, selector),
			sqlgraph.To(hpartist.Table, hpartist.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, hpelineupmallitem.TaggedArtistsTable, hpelineupmallitem.TaggedArtistsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(hemiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTaggedMembers chains the current query on the "tagged_members" edge.
func (hemiq *HPElineupMallItemQuery) QueryTaggedMembers() *HPMemberQuery {
	query := (&HPMemberClient{config: hemiq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hemiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hemiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hpelineupmallitem.Table, hpelineupmallitem.FieldID, selector),
			sqlgraph.To(hpmember.Table, hpmember.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, hpelineupmallitem.TaggedMembersTable, hpelineupmallitem.TaggedMembersPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(hemiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first HPElineupMallItem entity from the query.
// Returns a *NotFoundError when no HPElineupMallItem was found.
func (hemiq *HPElineupMallItemQuery) First(ctx context.Context) (*HPElineupMallItem, error) {
	nodes, err := hemiq.Limit(1).All(setContextOp(ctx, hemiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hpelineupmallitem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hemiq *HPElineupMallItemQuery) FirstX(ctx context.Context) *HPElineupMallItem {
	node, err := hemiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HPElineupMallItem ID from the query.
// Returns a *NotFoundError when no HPElineupMallItem ID was found.
func (hemiq *HPElineupMallItemQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hemiq.Limit(1).IDs(setContextOp(ctx, hemiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hpelineupmallitem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hemiq *HPElineupMallItemQuery) FirstIDX(ctx context.Context) int {
	id, err := hemiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HPElineupMallItem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HPElineupMallItem entity is found.
// Returns a *NotFoundError when no HPElineupMallItem entities are found.
func (hemiq *HPElineupMallItemQuery) Only(ctx context.Context) (*HPElineupMallItem, error) {
	nodes, err := hemiq.Limit(2).All(setContextOp(ctx, hemiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hpelineupmallitem.Label}
	default:
		return nil, &NotSingularError{hpelineupmallitem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hemiq *HPElineupMallItemQuery) OnlyX(ctx context.Context) *HPElineupMallItem {
	node, err := hemiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HPElineupMallItem ID in the query.
// Returns a *NotSingularError when more than one HPElineupMallItem ID is found.
// Returns a *NotFoundError when no entities are found.
func (hemiq *HPElineupMallItemQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hemiq.Limit(2).IDs(setContextOp(ctx, hemiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hpelineupmallitem.Label}
	default:
		err = &NotSingularError{hpelineupmallitem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hemiq *HPElineupMallItemQuery) OnlyIDX(ctx context.Context) int {
	id, err := hemiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HPElineupMallItems.
func (hemiq *HPElineupMallItemQuery) All(ctx context.Context) ([]*HPElineupMallItem, error) {
	ctx = setContextOp(ctx, hemiq.ctx, "All")
	if err := hemiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HPElineupMallItem, *HPElineupMallItemQuery]()
	return withInterceptors[[]*HPElineupMallItem](ctx, hemiq, qr, hemiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hemiq *HPElineupMallItemQuery) AllX(ctx context.Context) []*HPElineupMallItem {
	nodes, err := hemiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HPElineupMallItem IDs.
func (hemiq *HPElineupMallItemQuery) IDs(ctx context.Context) (ids []int, err error) {
	if hemiq.ctx.Unique == nil && hemiq.path != nil {
		hemiq.Unique(true)
	}
	ctx = setContextOp(ctx, hemiq.ctx, "IDs")
	if err = hemiq.Select(hpelineupmallitem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hemiq *HPElineupMallItemQuery) IDsX(ctx context.Context) []int {
	ids, err := hemiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hemiq *HPElineupMallItemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hemiq.ctx, "Count")
	if err := hemiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hemiq, querierCount[*HPElineupMallItemQuery](), hemiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hemiq *HPElineupMallItemQuery) CountX(ctx context.Context) int {
	count, err := hemiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hemiq *HPElineupMallItemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hemiq.ctx, "Exist")
	switch _, err := hemiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hemiq *HPElineupMallItemQuery) ExistX(ctx context.Context) bool {
	exist, err := hemiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HPElineupMallItemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hemiq *HPElineupMallItemQuery) Clone() *HPElineupMallItemQuery {
	if hemiq == nil {
		return nil
	}
	return &HPElineupMallItemQuery{
		config:            hemiq.config,
		ctx:               hemiq.ctx.Clone(),
		order:             append([]hpelineupmallitem.Order{}, hemiq.order...),
		inters:            append([]Interceptor{}, hemiq.inters...),
		predicates:        append([]predicate.HPElineupMallItem{}, hemiq.predicates...),
		withTaggedArtists: hemiq.withTaggedArtists.Clone(),
		withTaggedMembers: hemiq.withTaggedMembers.Clone(),
		// clone intermediate query.
		sql:  hemiq.sql.Clone(),
		path: hemiq.path,
	}
}

// WithTaggedArtists tells the query-builder to eager-load the nodes that are connected to
// the "tagged_artists" edge. The optional arguments are used to configure the query builder of the edge.
func (hemiq *HPElineupMallItemQuery) WithTaggedArtists(opts ...func(*HPArtistQuery)) *HPElineupMallItemQuery {
	query := (&HPArtistClient{config: hemiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hemiq.withTaggedArtists = query
	return hemiq
}

// WithTaggedMembers tells the query-builder to eager-load the nodes that are connected to
// the "tagged_members" edge. The optional arguments are used to configure the query builder of the edge.
func (hemiq *HPElineupMallItemQuery) WithTaggedMembers(opts ...func(*HPMemberQuery)) *HPElineupMallItemQuery {
	query := (&HPMemberClient{config: hemiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hemiq.withTaggedMembers = query
	return hemiq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CrawledAt time.Time `json:"crawled_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HPElineupMallItem.Query().
//		GroupBy(hpelineupmallitem.FieldCrawledAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (hemiq *HPElineupMallItemQuery) GroupBy(field string, fields ...string) *HPElineupMallItemGroupBy {
	hemiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HPElineupMallItemGroupBy{build: hemiq}
	grbuild.flds = &hemiq.ctx.Fields
	grbuild.label = hpelineupmallitem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CrawledAt time.Time `json:"crawled_at,omitempty"`
//	}
//
//	client.HPElineupMallItem.Query().
//		Select(hpelineupmallitem.FieldCrawledAt).
//		Scan(ctx, &v)
//
func (hemiq *HPElineupMallItemQuery) Select(fields ...string) *HPElineupMallItemSelect {
	hemiq.ctx.Fields = append(hemiq.ctx.Fields, fields...)
	sbuild := &HPElineupMallItemSelect{HPElineupMallItemQuery: hemiq}
	sbuild.label = hpelineupmallitem.Label
	sbuild.flds, sbuild.scan = &hemiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HPElineupMallItemSelect configured with the given aggregations.
func (hemiq *HPElineupMallItemQuery) Aggregate(fns ...AggregateFunc) *HPElineupMallItemSelect {
	return hemiq.Select().Aggregate(fns...)
}

func (hemiq *HPElineupMallItemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hemiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hemiq); err != nil {
				return err
			}
		}
	}
	for _, f := range hemiq.ctx.Fields {
		if !hpelineupmallitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hemiq.path != nil {
		prev, err := hemiq.path(ctx)
		if err != nil {
			return err
		}
		hemiq.sql = prev
	}
	return nil
}

func (hemiq *HPElineupMallItemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HPElineupMallItem, error) {
	var (
		nodes       = []*HPElineupMallItem{}
		_spec       = hemiq.querySpec()
		loadedTypes = [2]bool{
			hemiq.withTaggedArtists != nil,
			hemiq.withTaggedMembers != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HPElineupMallItem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HPElineupMallItem{config: hemiq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(hemiq.modifiers) > 0 {
		_spec.Modifiers = hemiq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hemiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := hemiq.withTaggedArtists; query != nil {
		if err := hemiq.loadTaggedArtists(ctx, query, nodes,
			func(n *HPElineupMallItem) { n.Edges.TaggedArtists = []*HPArtist{} },
			func(n *HPElineupMallItem, e *HPArtist) { n.Edges.TaggedArtists = append(n.Edges.TaggedArtists, e) }); err != nil {
			return nil, err
		}
	}
	if query := hemiq.withTaggedMembers; query != nil {
		if err := hemiq.loadTaggedMembers(ctx, query, nodes,
			func(n *HPElineupMallItem) { n.Edges.TaggedMembers = []*HPMember{} },
			func(n *HPElineupMallItem, e *HPMember) { n.Edges.TaggedMembers = append(n.Edges.TaggedMembers, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range hemiq.withNamedTaggedArtists {
		if err := hemiq.loadTaggedArtists(ctx, query, nodes,
			func(n *HPElineupMallItem) { n.appendNamedTaggedArtists(name) },
			func(n *HPElineupMallItem, e *HPArtist) { n.appendNamedTaggedArtists(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range hemiq.withNamedTaggedMembers {
		if err := hemiq.loadTaggedMembers(ctx, query, nodes,
			func(n *HPElineupMallItem) { n.appendNamedTaggedMembers(name) },
			func(n *HPElineupMallItem, e *HPMember) { n.appendNamedTaggedMembers(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range hemiq.loadTotal {
		if err := hemiq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (hemiq *HPElineupMallItemQuery) loadTaggedArtists(ctx context.Context, query *HPArtistQuery, nodes []*HPElineupMallItem, init func(*HPElineupMallItem), assign func(*HPElineupMallItem, *HPArtist)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*HPElineupMallItem)
	nids := make(map[int]map[*HPElineupMallItem]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(hpelineupmallitem.TaggedArtistsTable)
		s.Join(joinT).On(s.C(hpartist.FieldID), joinT.C(hpelineupmallitem.TaggedArtistsPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(hpelineupmallitem.TaggedArtistsPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(hpelineupmallitem.TaggedArtistsPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*HPElineupMallItem]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*HPArtist](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "tagged_artists" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (hemiq *HPElineupMallItemQuery) loadTaggedMembers(ctx context.Context, query *HPMemberQuery, nodes []*HPElineupMallItem, init func(*HPElineupMallItem), assign func(*HPElineupMallItem, *HPMember)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*HPElineupMallItem)
	nids := make(map[int]map[*HPElineupMallItem]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(hpelineupmallitem.TaggedMembersTable)
		s.Join(joinT).On(s.C(hpmember.FieldID), joinT.C(hpelineupmallitem.TaggedMembersPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(hpelineupmallitem.TaggedMembersPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(hpelineupmallitem.TaggedMembersPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*HPElineupMallItem]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*HPMember](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "tagged_members" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (hemiq *HPElineupMallItemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hemiq.querySpec()
	if len(hemiq.modifiers) > 0 {
		_spec.Modifiers = hemiq.modifiers
	}
	_spec.Node.Columns = hemiq.ctx.Fields
	if len(hemiq.ctx.Fields) > 0 {
		_spec.Unique = hemiq.ctx.Unique != nil && *hemiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hemiq.driver, _spec)
}

func (hemiq *HPElineupMallItemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hpelineupmallitem.Table, hpelineupmallitem.Columns, sqlgraph.NewFieldSpec(hpelineupmallitem.FieldID, field.TypeInt))
	_spec.From = hemiq.sql
	if unique := hemiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hemiq.path != nil {
		_spec.Unique = true
	}
	if fields := hemiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hpelineupmallitem.FieldID)
		for i := range fields {
			if fields[i] != hpelineupmallitem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hemiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hemiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hemiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hemiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hemiq *HPElineupMallItemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hemiq.driver.Dialect())
	t1 := builder.Table(hpelineupmallitem.Table)
	columns := hemiq.ctx.Fields
	if len(columns) == 0 {
		columns = hpelineupmallitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hemiq.sql != nil {
		selector = hemiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hemiq.ctx.Unique != nil && *hemiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hemiq.predicates {
		p(selector)
	}
	for _, p := range hemiq.order {
		p(selector)
	}
	if offset := hemiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hemiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedTaggedArtists tells the query-builder to eager-load the nodes that are connected to the "tagged_artists"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (hemiq *HPElineupMallItemQuery) WithNamedTaggedArtists(name string, opts ...func(*HPArtistQuery)) *HPElineupMallItemQuery {
	query := (&HPArtistClient{config: hemiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if hemiq.withNamedTaggedArtists == nil {
		hemiq.withNamedTaggedArtists = make(map[string]*HPArtistQuery)
	}
	hemiq.withNamedTaggedArtists[name] = query
	return hemiq
}

// WithNamedTaggedMembers tells the query-builder to eager-load the nodes that are connected to the "tagged_members"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (hemiq *HPElineupMallItemQuery) WithNamedTaggedMembers(name string, opts ...func(*HPMemberQuery)) *HPElineupMallItemQuery {
	query := (&HPMemberClient{config: hemiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if hemiq.withNamedTaggedMembers == nil {
		hemiq.withNamedTaggedMembers = make(map[string]*HPMemberQuery)
	}
	hemiq.withNamedTaggedMembers[name] = query
	return hemiq
}

// HPElineupMallItemGroupBy is the group-by builder for HPElineupMallItem entities.
type HPElineupMallItemGroupBy struct {
	selector
	build *HPElineupMallItemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hemigb *HPElineupMallItemGroupBy) Aggregate(fns ...AggregateFunc) *HPElineupMallItemGroupBy {
	hemigb.fns = append(hemigb.fns, fns...)
	return hemigb
}

// Scan applies the selector query and scans the result into the given value.
func (hemigb *HPElineupMallItemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hemigb.build.ctx, "GroupBy")
	if err := hemigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HPElineupMallItemQuery, *HPElineupMallItemGroupBy](ctx, hemigb.build, hemigb, hemigb.build.inters, v)
}

func (hemigb *HPElineupMallItemGroupBy) sqlScan(ctx context.Context, root *HPElineupMallItemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hemigb.fns))
	for _, fn := range hemigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hemigb.flds)+len(hemigb.fns))
		for _, f := range *hemigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hemigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hemigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HPElineupMallItemSelect is the builder for selecting fields of HPElineupMallItem entities.
type HPElineupMallItemSelect struct {
	*HPElineupMallItemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hemis *HPElineupMallItemSelect) Aggregate(fns ...AggregateFunc) *HPElineupMallItemSelect {
	hemis.fns = append(hemis.fns, fns...)
	return hemis
}

// Scan applies the selector query and scans the result into the given value.
func (hemis *HPElineupMallItemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hemis.ctx, "Select")
	if err := hemis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HPElineupMallItemQuery, *HPElineupMallItemSelect](ctx, hemis.HPElineupMallItemQuery, hemis, hemis.inters, v)
}

func (hemis *HPElineupMallItemSelect) sqlScan(ctx context.Context, root *HPElineupMallItemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hemis.fns))
	for _, fn := range hemis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hemis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hemis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
