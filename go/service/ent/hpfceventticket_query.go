// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yssk22/hpapp/go/service/ent/hpevent"
	"github.com/yssk22/hpapp/go/service/ent/hpfceventticket"
	"github.com/yssk22/hpapp/go/service/ent/predicate"
	"github.com/yssk22/hpapp/go/service/ent/user"
)

// HPFCEventTicketQuery is the builder for querying HPFCEventTicket entities.
type HPFCEventTicketQuery struct {
	config
	ctx        *QueryContext
	order      []hpfceventticket.Order
	inters     []Interceptor
	predicates []predicate.HPFCEventTicket
	withEvent  *HPEventQuery
	withUser   *UserQuery
	withFKs    bool
	modifiers  []func(*sql.Selector)
	loadTotal  []func(context.Context, []*HPFCEventTicket) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HPFCEventTicketQuery builder.
func (hetq *HPFCEventTicketQuery) Where(ps ...predicate.HPFCEventTicket) *HPFCEventTicketQuery {
	hetq.predicates = append(hetq.predicates, ps...)
	return hetq
}

// Limit the number of records to be returned by this query.
func (hetq *HPFCEventTicketQuery) Limit(limit int) *HPFCEventTicketQuery {
	hetq.ctx.Limit = &limit
	return hetq
}

// Offset to start from.
func (hetq *HPFCEventTicketQuery) Offset(offset int) *HPFCEventTicketQuery {
	hetq.ctx.Offset = &offset
	return hetq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hetq *HPFCEventTicketQuery) Unique(unique bool) *HPFCEventTicketQuery {
	hetq.ctx.Unique = &unique
	return hetq
}

// Order specifies how the records should be ordered.
func (hetq *HPFCEventTicketQuery) Order(o ...hpfceventticket.Order) *HPFCEventTicketQuery {
	hetq.order = append(hetq.order, o...)
	return hetq
}

// QueryEvent chains the current query on the "event" edge.
func (hetq *HPFCEventTicketQuery) QueryEvent() *HPEventQuery {
	query := (&HPEventClient{config: hetq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hetq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hetq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hpfceventticket.Table, hpfceventticket.FieldID, selector),
			sqlgraph.To(hpevent.Table, hpevent.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, hpfceventticket.EventTable, hpfceventticket.EventColumn),
		)
		fromU = sqlgraph.SetNeighbors(hetq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (hetq *HPFCEventTicketQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: hetq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hetq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hetq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hpfceventticket.Table, hpfceventticket.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, hpfceventticket.UserTable, hpfceventticket.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(hetq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first HPFCEventTicket entity from the query.
// Returns a *NotFoundError when no HPFCEventTicket was found.
func (hetq *HPFCEventTicketQuery) First(ctx context.Context) (*HPFCEventTicket, error) {
	nodes, err := hetq.Limit(1).All(setContextOp(ctx, hetq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hpfceventticket.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hetq *HPFCEventTicketQuery) FirstX(ctx context.Context) *HPFCEventTicket {
	node, err := hetq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HPFCEventTicket ID from the query.
// Returns a *NotFoundError when no HPFCEventTicket ID was found.
func (hetq *HPFCEventTicketQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hetq.Limit(1).IDs(setContextOp(ctx, hetq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hpfceventticket.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hetq *HPFCEventTicketQuery) FirstIDX(ctx context.Context) int {
	id, err := hetq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HPFCEventTicket entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HPFCEventTicket entity is found.
// Returns a *NotFoundError when no HPFCEventTicket entities are found.
func (hetq *HPFCEventTicketQuery) Only(ctx context.Context) (*HPFCEventTicket, error) {
	nodes, err := hetq.Limit(2).All(setContextOp(ctx, hetq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hpfceventticket.Label}
	default:
		return nil, &NotSingularError{hpfceventticket.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hetq *HPFCEventTicketQuery) OnlyX(ctx context.Context) *HPFCEventTicket {
	node, err := hetq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HPFCEventTicket ID in the query.
// Returns a *NotSingularError when more than one HPFCEventTicket ID is found.
// Returns a *NotFoundError when no entities are found.
func (hetq *HPFCEventTicketQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hetq.Limit(2).IDs(setContextOp(ctx, hetq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hpfceventticket.Label}
	default:
		err = &NotSingularError{hpfceventticket.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hetq *HPFCEventTicketQuery) OnlyIDX(ctx context.Context) int {
	id, err := hetq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HPFCEventTickets.
func (hetq *HPFCEventTicketQuery) All(ctx context.Context) ([]*HPFCEventTicket, error) {
	ctx = setContextOp(ctx, hetq.ctx, "All")
	if err := hetq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HPFCEventTicket, *HPFCEventTicketQuery]()
	return withInterceptors[[]*HPFCEventTicket](ctx, hetq, qr, hetq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hetq *HPFCEventTicketQuery) AllX(ctx context.Context) []*HPFCEventTicket {
	nodes, err := hetq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HPFCEventTicket IDs.
func (hetq *HPFCEventTicketQuery) IDs(ctx context.Context) (ids []int, err error) {
	if hetq.ctx.Unique == nil && hetq.path != nil {
		hetq.Unique(true)
	}
	ctx = setContextOp(ctx, hetq.ctx, "IDs")
	if err = hetq.Select(hpfceventticket.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hetq *HPFCEventTicketQuery) IDsX(ctx context.Context) []int {
	ids, err := hetq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hetq *HPFCEventTicketQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hetq.ctx, "Count")
	if err := hetq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hetq, querierCount[*HPFCEventTicketQuery](), hetq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hetq *HPFCEventTicketQuery) CountX(ctx context.Context) int {
	count, err := hetq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hetq *HPFCEventTicketQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hetq.ctx, "Exist")
	switch _, err := hetq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hetq *HPFCEventTicketQuery) ExistX(ctx context.Context) bool {
	exist, err := hetq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HPFCEventTicketQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hetq *HPFCEventTicketQuery) Clone() *HPFCEventTicketQuery {
	if hetq == nil {
		return nil
	}
	return &HPFCEventTicketQuery{
		config:     hetq.config,
		ctx:        hetq.ctx.Clone(),
		order:      append([]hpfceventticket.Order{}, hetq.order...),
		inters:     append([]Interceptor{}, hetq.inters...),
		predicates: append([]predicate.HPFCEventTicket{}, hetq.predicates...),
		withEvent:  hetq.withEvent.Clone(),
		withUser:   hetq.withUser.Clone(),
		// clone intermediate query.
		sql:  hetq.sql.Clone(),
		path: hetq.path,
	}
}

// WithEvent tells the query-builder to eager-load the nodes that are connected to
// the "event" edge. The optional arguments are used to configure the query builder of the edge.
func (hetq *HPFCEventTicketQuery) WithEvent(opts ...func(*HPEventQuery)) *HPFCEventTicketQuery {
	query := (&HPEventClient{config: hetq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hetq.withEvent = query
	return hetq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (hetq *HPFCEventTicketQuery) WithUser(opts ...func(*UserQuery)) *HPFCEventTicketQuery {
	query := (&UserClient{config: hetq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hetq.withUser = query
	return hetq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HPFCEventTicket.Query().
//		GroupBy(hpfceventticket.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (hetq *HPFCEventTicketQuery) GroupBy(field string, fields ...string) *HPFCEventTicketGroupBy {
	hetq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HPFCEventTicketGroupBy{build: hetq}
	grbuild.flds = &hetq.ctx.Fields
	grbuild.label = hpfceventticket.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.HPFCEventTicket.Query().
//		Select(hpfceventticket.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (hetq *HPFCEventTicketQuery) Select(fields ...string) *HPFCEventTicketSelect {
	hetq.ctx.Fields = append(hetq.ctx.Fields, fields...)
	sbuild := &HPFCEventTicketSelect{HPFCEventTicketQuery: hetq}
	sbuild.label = hpfceventticket.Label
	sbuild.flds, sbuild.scan = &hetq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HPFCEventTicketSelect configured with the given aggregations.
func (hetq *HPFCEventTicketQuery) Aggregate(fns ...AggregateFunc) *HPFCEventTicketSelect {
	return hetq.Select().Aggregate(fns...)
}

func (hetq *HPFCEventTicketQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hetq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hetq); err != nil {
				return err
			}
		}
	}
	for _, f := range hetq.ctx.Fields {
		if !hpfceventticket.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hetq.path != nil {
		prev, err := hetq.path(ctx)
		if err != nil {
			return err
		}
		hetq.sql = prev
	}
	if hpfceventticket.Policy == nil {
		return errors.New("ent: uninitialized hpfceventticket.Policy (forgotten import ent/runtime?)")
	}
	if err := hpfceventticket.Policy.EvalQuery(ctx, hetq); err != nil {
		return err
	}
	return nil
}

func (hetq *HPFCEventTicketQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HPFCEventTicket, error) {
	var (
		nodes       = []*HPFCEventTicket{}
		withFKs     = hetq.withFKs
		_spec       = hetq.querySpec()
		loadedTypes = [2]bool{
			hetq.withEvent != nil,
			hetq.withUser != nil,
		}
	)
	if hetq.withEvent != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, hpfceventticket.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HPFCEventTicket).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HPFCEventTicket{config: hetq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(hetq.modifiers) > 0 {
		_spec.Modifiers = hetq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hetq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := hetq.withEvent; query != nil {
		if err := hetq.loadEvent(ctx, query, nodes, nil,
			func(n *HPFCEventTicket, e *HPEvent) { n.Edges.Event = e }); err != nil {
			return nil, err
		}
	}
	if query := hetq.withUser; query != nil {
		if err := hetq.loadUser(ctx, query, nodes, nil,
			func(n *HPFCEventTicket, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	for i := range hetq.loadTotal {
		if err := hetq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (hetq *HPFCEventTicketQuery) loadEvent(ctx context.Context, query *HPEventQuery, nodes []*HPFCEventTicket, init func(*HPFCEventTicket), assign func(*HPFCEventTicket, *HPEvent)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*HPFCEventTicket)
	for i := range nodes {
		if nodes[i].hp_event_hpfc_event_tickets == nil {
			continue
		}
		fk := *nodes[i].hp_event_hpfc_event_tickets
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(hpevent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "hp_event_hpfc_event_tickets" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (hetq *HPFCEventTicketQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*HPFCEventTicket, init func(*HPFCEventTicket), assign func(*HPFCEventTicket, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*HPFCEventTicket)
	for i := range nodes {
		fk := nodes[i].OwnerUserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "owner_user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (hetq *HPFCEventTicketQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hetq.querySpec()
	if len(hetq.modifiers) > 0 {
		_spec.Modifiers = hetq.modifiers
	}
	_spec.Node.Columns = hetq.ctx.Fields
	if len(hetq.ctx.Fields) > 0 {
		_spec.Unique = hetq.ctx.Unique != nil && *hetq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hetq.driver, _spec)
}

func (hetq *HPFCEventTicketQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hpfceventticket.Table, hpfceventticket.Columns, sqlgraph.NewFieldSpec(hpfceventticket.FieldID, field.TypeInt))
	_spec.From = hetq.sql
	if unique := hetq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hetq.path != nil {
		_spec.Unique = true
	}
	if fields := hetq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hpfceventticket.FieldID)
		for i := range fields {
			if fields[i] != hpfceventticket.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if hetq.withUser != nil {
			_spec.Node.AddColumnOnce(hpfceventticket.FieldOwnerUserID)
		}
	}
	if ps := hetq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hetq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hetq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hetq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hetq *HPFCEventTicketQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hetq.driver.Dialect())
	t1 := builder.Table(hpfceventticket.Table)
	columns := hetq.ctx.Fields
	if len(columns) == 0 {
		columns = hpfceventticket.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hetq.sql != nil {
		selector = hetq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hetq.ctx.Unique != nil && *hetq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hetq.predicates {
		p(selector)
	}
	for _, p := range hetq.order {
		p(selector)
	}
	if offset := hetq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hetq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HPFCEventTicketGroupBy is the group-by builder for HPFCEventTicket entities.
type HPFCEventTicketGroupBy struct {
	selector
	build *HPFCEventTicketQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hetgb *HPFCEventTicketGroupBy) Aggregate(fns ...AggregateFunc) *HPFCEventTicketGroupBy {
	hetgb.fns = append(hetgb.fns, fns...)
	return hetgb
}

// Scan applies the selector query and scans the result into the given value.
func (hetgb *HPFCEventTicketGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hetgb.build.ctx, "GroupBy")
	if err := hetgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HPFCEventTicketQuery, *HPFCEventTicketGroupBy](ctx, hetgb.build, hetgb, hetgb.build.inters, v)
}

func (hetgb *HPFCEventTicketGroupBy) sqlScan(ctx context.Context, root *HPFCEventTicketQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hetgb.fns))
	for _, fn := range hetgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hetgb.flds)+len(hetgb.fns))
		for _, f := range *hetgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hetgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hetgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HPFCEventTicketSelect is the builder for selecting fields of HPFCEventTicket entities.
type HPFCEventTicketSelect struct {
	*HPFCEventTicketQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hets *HPFCEventTicketSelect) Aggregate(fns ...AggregateFunc) *HPFCEventTicketSelect {
	hets.fns = append(hets.fns, fns...)
	return hets
}

// Scan applies the selector query and scans the result into the given value.
func (hets *HPFCEventTicketSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hets.ctx, "Select")
	if err := hets.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HPFCEventTicketQuery, *HPFCEventTicketSelect](ctx, hets.HPFCEventTicketQuery, hets, hets.inters, v)
}

func (hets *HPFCEventTicketSelect) sqlScan(ctx context.Context, root *HPFCEventTicketQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hets.fns))
	for _, fn := range hets.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hets.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hets.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
