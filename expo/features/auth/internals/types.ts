export type UserRole = 'admin' | 'developer' | 'fcmember' | 'user' | 'guest';
// eslint-disable-next-line no-unused-vars
export type UserRoleMap = {
  admin: boolean;
  developer: boolean;
  fcmember: boolean;
  user: boolean;
  guest: boolean;
};
